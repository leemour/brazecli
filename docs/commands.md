# Commands

GENERATED by `pnpm docs:generate` from the CLI itself. Do not edit by hand.

Every command below is walked out of the live program, so this file and
`braze commands --json` can never disagree about what exists.

107 runnable commands: 12 written by hand, 95 generated from [the Braze collection](../spec/braze.postman.json).

**A command that talks to Braze names its profile as the first word** — `braze staging
campaigns list`. There is no default profile, deliberately (`NEED-25`). The commands that
never reach Braze — `profile`, `runs`, `commands`, `schema`, `skill` — take none.

## Global options

| Option | Required | |
|---|---|---|
| `-V, --version` | no | output the version number |
| `--profile <name>` | no | which configured profile to use |
| `--json` | no | one deterministic JSON value on stdout, whatever the terminal is |
| `--output <format>` | no | output mode |
| `--no-color` | no | never emit ANSI colour |
| `--dry-run` | no | resolve, validate and count, but send nothing |
| `--confirm` | no | required before any write; never an interactive prompt |
| `--paginate` | no | walk the pages of a paged read and return them as one value |
| `--max-pages <n>` | no | how many pages --paginate may walk (default 10) |
| `--max-items <n>` | no | stop --paginate once this many rows have been collected |
| `--records <source>` | no | a file of many records to batch and send, or - for standard input |
| `--records-format <format>` | no | how --records is encoded |
| `--records-field <name>` | no | which batch field the records belong in, e.g. attributes |
| `--record-id <key>` | no | key of each record holding your own id for it, for the audit |
| `--concurrency <n>` | no | how many requests a bulk run may have in flight, 1-32 |
| `--runs-dir <path>` | no | where run artifacts are written |
| `--timeout <ms>` | no | per-attempt timeout in milliseconds |
| `--retries <n>` | no | attempts after the first |

## Written by hand

### `braze profile add`

add or update a profile and store its API key

| Argument | Required | |
|---|---|---|
| `name` | yes | profile name, such as production or staging |

| Option | Required | |
|---|---|---|
| `--endpoint <url>` | no | Braze REST endpoint, e.g. https://rest.fra-01.braze.eu |
| `--read-only` | no | refuse every write for this profile, whatever flags a command carries |
| `--no-read-only` | no | allow writes again; they still need --confirm |
| `--key-stdin` | no | read the API key from standard input, for a CI with no terminal |

### `braze profile verify`

check that a profile still points at the workspace you think it does

| Argument | Required | |
|---|---|---|
| `name` | no | profile to check; otherwise --profile or BRAZE_PROFILE |

| Option | Required | |
|---|---|---|
| `--expect-max <count>` | no | record this ceiling on monthly active users |

### `braze profile list`

show the configured profiles

### `braze profile remove`

remove a profile and its stored key

| Argument | Required | |
|---|---|---|
| `name` | yes | profile to remove |

### `braze api`

send any Braze request, typed or not — the escape hatch

| Argument | Required | |
|---|---|---|
| `method` | yes | one of GET, POST, PUT, PATCH, DELETE, HEAD |
| `path` | yes | a Braze path such as /campaigns/list — never a whole URL |

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | repeatable query parameter |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze runs list`

list past runs, newest first

| Option | Required | |
|---|---|---|
| `--limit <n>` | no | how many to show, newest first |

### `braze runs show`

everything recorded about one run

| Argument | Required | |
|---|---|---|
| `run-id` | yes | id from `braze runs list` |

### `braze runs path`

the directory holding a run's artifacts

| Argument | Required | |
|---|---|---|
| `run-id` | yes | id from `braze runs list` |

### `braze runs cleanup`

remove old run directories — opt-in, never automatic

| Option | Required | |
|---|---|---|
| `--older-than <days>` | yes | remove runs that started more than this many days ago |
| `--dry-run` | no | list what would be removed and remove nothing |
| `--confirm` | no | actually remove them |

### `braze commands`

every command, option and exit code as JSON — the discovery surface for an agent

### `braze schema`

everything needed to call one operation: parameters, body, and whether it writes

| Argument | Required | |
|---|---|---|
| `operation` | yes | an operation id (campaigns.list.get) or the command words (campaigns list) |

### `braze skill install`

write SKILL.md where an agent will find it

| Option | Required | |
|---|---|---|
| `--claude` | no | install for Claude Code |
| `--codex` | no | install for Codex |
| `--hermes` | no | install for Hermes |
| `--project` | no | install into this repository rather than your home directory |
| `--dir <path>` | no | a skills directory of your own; the skill lands in <path>/braze/ |

## Generated from the catalog

Each of these is one Braze endpoint. `braze schema <operation>` prints its full contract,
including the request body where Braze documents one.

### `braze <profile> catalogs delete`

Use this endpoint to delete a catalog.

Operation `catalogs.by-id.delete`.

| Option | Required | |
|---|---|---|
| `--catalog-name <value>` | yes | path parameter {catalog_name} |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> catalogs get`

Use this endpoint to return a list of catalogs in a workspace.

Operation `catalogs.get`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> catalogs create`

Use this endpoint to create a catalog.

Operation `catalogs.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> catalogs items delete-many`

Use this endpoint to delete multiple items in your catalog.

Operation `catalogs.by-id.items.delete`.

| Option | Required | |
|---|---|---|
| `--catalog-name <value>` | yes | path parameter {catalog_name} |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> catalogs items update-many`

Use this endpoint to edit multiple items in your catalog.

Operation `catalogs.by-id.items.update`.

| Option | Required | |
|---|---|---|
| `--catalog-name <value>` | yes | path parameter {catalog_name} |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> catalogs items create-many`

Use this endpoint to create multiple items in your catalog.

Operation `catalogs.by-id.items.create`.

| Option | Required | |
|---|---|---|
| `--catalog-name <value>` | yes | path parameter {catalog_name} |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> catalogs items replace-many`

Use this endpoint to update multiple items in your catalog.

Operation `catalogs.by-id.items.replace`.

| Option | Required | |
|---|---|---|
| `--catalog-name <value>` | yes | path parameter {catalog_name} |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> catalogs items delete`

Use this endpoint to delete an item in your catalog.

Operation `catalogs.by-id.items.by-id.delete`.

| Option | Required | |
|---|---|---|
| `--catalog-name <value>` | yes | path parameter {catalog_name} |
| `--item-id <value>` | yes | path parameter {item_id} |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> catalogs items get`

Use this endpoint to return a catalog item and its content.

Operation `catalogs.by-id.items.by-id.get`.

| Option | Required | |
|---|---|---|
| `--catalog-name <value>` | yes | path parameter {catalog_name} |
| `--item-id <value>` | yes | path parameter {item_id} |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> catalogs items list`

Use this endpoint to return multiple catalog items and their content.

Operation `catalogs.by-id.items.get`.

| Option | Required | |
|---|---|---|
| `--catalog-name <value>` | yes | path parameter {catalog_name} |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> catalogs items update`

Use this endpoint to edit an item in your catalog.

Operation `catalogs.by-id.items.by-id.update`.

| Option | Required | |
|---|---|---|
| `--catalog-name <value>` | yes | path parameter {catalog_name} |
| `--item-id <value>` | yes | path parameter {item_id} |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> catalogs items create`

Use this endpoint to create an item in your catalog.

Operation `catalogs.by-id.items.by-id.create`.

| Option | Required | |
|---|---|---|
| `--catalog-name <value>` | yes | path parameter {catalog_name} |
| `--item-id <value>` | yes | path parameter {item_id} |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> catalogs items replace`

Use this endpoint to update an item in your catalog.

Operation `catalogs.by-id.items.by-id.replace`.

| Option | Required | |
|---|---|---|
| `--catalog-name <value>` | yes | path parameter {catalog_name} |
| `--item-id <value>` | yes | path parameter {item_id} |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> email hard-bounces`

Use this endpoint to pull a list of email addresses that have “hard bounced” your email messages within a certain time frame.

Operation `email.hard-bounces.get`.

| Option | Required | |
|---|---|---|
| `--start-date <value>` | no | first day of the range, as YYYY-MM-DD. Must be earlier than --end-date (e.g. 2019-01-01) |
| `--end-date <value>` | no | last day of the range, as YYYY-MM-DD (e.g. 2019-02-01) |
| `--limit <value>` | no | how many results to return. Defaults to 100; the cap differs by endpoint — 500 for email lists, 1000 for Content Blocks (e.g. 100) |
| `--offset <value>` | no | how many results to skip before returning the rest (e.g. 1) |
| `--email <value>` | no | an email address. Where it is a filter, Braze answers for that address and ignores the date range (e.g. example@braze.com) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> email unsubscribes`

Use this endpoint to return emails that have unsubscribed during the time period from start_date to end_date .

Operation `email.unsubscribes.get`.

| Option | Required | |
|---|---|---|
| `--start-date <value>` | no | first day of the range, as YYYY-MM-DD. Must be earlier than --end-date (e.g. 2020-01-01) |
| `--end-date <value>` | no | last day of the range, as YYYY-MM-DD (e.g. 2020-02-01) |
| `--limit <value>` | no | how many results to return. Defaults to 100; the cap differs by endpoint — 500 for email lists, 1000 for Content Blocks (e.g. 1) |
| `--offset <value>` | no | how many results to skip before returning the rest (e.g. 1) |
| `--sort-direction <value>` | no | desc for newest first, asc for oldest first. Defaults to oldest first (e.g. desc) |
| `--email <value>` | no | an email address. Where it is a filter, Braze answers for that address and ignores the date range (e.g. example@braze.com) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> email status`

Use this endpoint to set the email subscription state for your users.

Operation `email.status.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> email bounce remove`

Use this endpoint to remove email addresses from your Braze bounce list and bounce list maintained by your email provider.

Operation `email.bounce.remove.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> email spam remove`

Use this endpoint to remove email addresses from your Braze spam list and spam list maintained by your email provider.

Operation `email.spam.remove.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> email blocklist`

Use this endpoint to unsubscribe a user from email and mark them as hard bounced.

Operation `email.blocklist.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> email blacklist`

Use this endpoint to unsubscribe a user from email and mark them as hard bounced.

Operation `email.blacklist.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> campaigns data-series`

Use this endpoint to retrieve a daily series of various stats for a campaign over time.

Operation `campaigns.data-series.get`.

| Option | Required | |
|---|---|---|
| `--campaign-id <value>` | no | campaign API identifier |
| `--length <value>` | no | how many units before ending_at the series covers — days, or hours where the endpoint takes --unit. The cap differs by endpoint: 100 for most, 14 for Canvas (e.g. 7) |
| `--ending-at <value>` | no | date the series ends on, ISO-8601. Defaults to the time of the request (e.g. 2020-06-28T23:59:59-5:00) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> campaigns details`

Use this endpoint to retrieve relevant information on a specified campaign, which can be identified by the campaign_id .

Operation `campaigns.details.get`.

| Option | Required | |
|---|---|---|
| `--campaign-id <value>` | no | campaign API identifier |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> campaigns list`

Use this endpoint to export a list of campaigns, each of which will include its name, campaign API identifier, whether it is an API campaign, and tags associated with the campaign.

Operation `campaigns.list.get`.

| Option | Required | |
|---|---|---|
| `--page <value>` | no | which page to return, counting from 0. A page holds up to 100 (e.g. 0) |
| `--include-archived <value>` | no | include archived items. Defaults to false (e.g. false) |
| `--sort-direction <value>` | no | desc for newest first, asc for oldest first. Defaults to oldest first (e.g. desc) |
| `--last-edit-time-gt <value>` | no | only items edited after this time, as yyyy-MM-DDTHH:mm:ss (e.g. 2020-06-28T23:59:59-5:00) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> campaigns trigger schedule delete`

The delete schedule endpoint allows you to cancel a message that you previously scheduled API-triggered Canvases before it has been sent.

Operation `campaigns.trigger.schedule.delete.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> campaigns trigger schedule create`

Use this endpoint to send dashboard created campaign messages via API-triggered delivery, allowing you to decide what action should trigger the message to be sent.

Operation `campaigns.trigger.schedule.create.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> campaigns trigger schedule update`

Use this endpoint to update scheduled API-triggered campaigns created in the dashboard, allowing you to decide what action should trigger the message to be sent.

Operation `campaigns.trigger.schedule.update.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> campaigns trigger send`

Use this endpoint to send immediate, ad-hoc messages to designated users via API-triggered delivery.

Operation `campaigns.trigger.send.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> sends data-series`

Use this endpoint to retrieve a daily series of various stats for a tracked send_id for API campaigns.

Operation `sends.data-series.get`.

| Option | Required | |
|---|---|---|
| `--campaign-id <value>` | no | campaign API identifier |
| `--send-id <value>` | no | send API identifier, set when the message was sent |
| `--length <value>` | no | how many units before ending_at the series covers — days, or hours where the endpoint takes --unit. The cap differs by endpoint: 100 for most, 14 for Canvas (e.g. 30) |
| `--ending-at <value>` | no | date the series ends on, ISO-8601. Defaults to the time of the request (e.g. 2014-12-10T23:59:59-05:00) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> sends id create`

Use this endpoint to create send IDs that can be used to send messages and track message performance programatically, without campaign creation for each send.

Operation `sends.id.create.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> canvas data-series`

Use this endpoint to export time series data for a Canvas.

Operation `canvas.data-series.get`.

| Option | Required | |
|---|---|---|
| `--canvas-id <value>` | no | Canvas API identifier |
| `--ending-at <value>` | no | date the series ends on, ISO-8601. Defaults to the time of the request (e.g. 2018-05-30T23:59:59-5:00) |
| `--starting-at <value>` | no | date the series begins on, ISO-8601. An alternative to --length, not a companion to it (e.g. 2018-05-28T23:59:59-5:00) |
| `--length <value>` | no | how many units before ending_at the series covers — days, or hours where the endpoint takes --unit. The cap differs by endpoint: 100 for most, 14 for Canvas (e.g. 10) |
| `--include-variant-breakdown <value>` | no | also return per-variant statistics. Defaults to false (e.g. true) |
| `--include-step-breakdown <value>` | no | also return per-step statistics. Defaults to false (e.g. true) |
| `--include-deleted-step-data <value>` | no | also return statistics for steps that have been deleted. Defaults to false (e.g. true) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> canvas data-summary`

Use this endpoint to export rollups of time series data for a Canvas, providing a concise summary of a Canvas’ results.

Operation `canvas.data-summary.get`.

| Option | Required | |
|---|---|---|
| `--canvas-id <value>` | no | Canvas API identifier |
| `--ending-at <value>` | no | date the series ends on, ISO-8601. Defaults to the time of the request (e.g. 2018-05-30T23:59:59-5:00) |
| `--starting-at <value>` | no | date the series begins on, ISO-8601. An alternative to --length, not a companion to it (e.g. 2018-05-28T23:59:59-5:00) |
| `--length <value>` | no | how many units before ending_at the series covers — days, or hours where the endpoint takes --unit. The cap differs by endpoint: 100 for most, 14 for Canvas (e.g. 5) |
| `--include-variant-breakdown <value>` | no | also return per-variant statistics. Defaults to false (e.g. true) |
| `--include-step-breakdown <value>` | no | also return per-step statistics. Defaults to false (e.g. true) |
| `--include-deleted-step-data <value>` | no | also return statistics for steps that have been deleted. Defaults to false (e.g. true) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> canvas details`

Use this endpoint to export metadata about a Canvas, such as the name, time created, current status, and more.

Operation `canvas.details.get`.

| Option | Required | |
|---|---|---|
| `--canvas-id <value>` | no | Canvas API identifier |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> canvas list`

Use this endpoint to export a list of Canvases, including the name, Canvas API identifier and associated tags.

Operation `canvas.list.get`.

| Option | Required | |
|---|---|---|
| `--page <value>` | no | which page to return, counting from 0. A page holds up to 100 (e.g. 1) |
| `--include-archived <value>` | no | include archived items. Defaults to false (e.g. false) |
| `--sort-direction <value>` | no | desc for newest first, asc for oldest first. Defaults to oldest first (e.g. desc) |
| `--last-edit-time-gt <value>` | no | only items edited after this time, as yyyy-MM-DDTHH:mm:ss (e.g. 2020-06-28T23:59:59-5:00) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> canvas trigger schedule delete`

Use this endpoint to cancel a Canvas message that you previously scheduled via API-triggered before it has been sent.

Operation `canvas.trigger.schedule.delete.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> canvas trigger schedule create`

Use this endpoint to schedule Canvas messages via API-triggered delivery, allowing you to decide what action should trigger the message to be sent.

Operation `canvas.trigger.schedule.create.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> canvas trigger schedule update`

Use this endpoint to update scheduled API-triggered Canvases that were created in the dashboard.

Operation `canvas.trigger.schedule.update.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> canvas trigger send`

Use this endpoint to send Canvas messages via API-triggered delivery.

Operation `canvas.trigger.send.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> events list`

Use this endpoint to export a list of custom events that have been recorded for your app.

Operation `events.list.get`.

| Option | Required | |
|---|---|---|
| `--page <value>` | no | which page to return, counting from 0. A page holds up to 100 (e.g. 3) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> events data-series`

Use this endpoint to retrieve a series of the number of occurrences of a custom event in your app over a designated time period.

Operation `events.data-series.get`.

| Option | Required | |
|---|---|---|
| `--event <value>` | no | name of the custom event to return analytics for (e.g. event_name) |
| `--length <value>` | no | how many units before ending_at the series covers — days, or hours where the endpoint takes --unit. The cap differs by endpoint: 100 for most, 14 for Canvas (e.g. 24) |
| `--unit <value>` | no | time between data points: day or hour. Defaults to day (e.g. hour) |
| `--ending-at <value>` | no | date the series ends on, ISO-8601. Defaults to the time of the request (e.g. 2014-12-10T23:59:59-05:00) |
| `--app-id <value>` | no | app API identifier, to limit the analytics to one app |
| `--segment-id <value>` | no | segment API identifier, to limit the analytics to one analytics-enabled segment |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> kpi new-users data-series`

Use this endpoint to retrieve a daily series of the total number of new users on each date.

Operation `kpi.new-users.data-series.get`.

| Option | Required | |
|---|---|---|
| `--length <value>` | no | how many units before ending_at the series covers — days, or hours where the endpoint takes --unit. The cap differs by endpoint: 100 for most, 14 for Canvas (e.g. 14) |
| `--ending-at <value>` | no | date the series ends on, ISO-8601. Defaults to the time of the request (e.g. 2018-06-28T23:59:59-5:00) |
| `--app-id <value>` | no | app API identifier, to limit the analytics to one app |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> kpi dau data-series`

Use this endpoint to retrieve a daily series of the total number of unique active users on each date.

Operation `kpi.dau.data-series.get`.

| Option | Required | |
|---|---|---|
| `--length <value>` | no | how many units before ending_at the series covers — days, or hours where the endpoint takes --unit. The cap differs by endpoint: 100 for most, 14 for Canvas (e.g. 10) |
| `--ending-at <value>` | no | date the series ends on, ISO-8601. Defaults to the time of the request (e.g. 2018-06-28T23:59:59-5:00) |
| `--app-id <value>` | no | app API identifier, to limit the analytics to one app |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> kpi mau data-series`

Use this endpoint to retrieve a daily series of the total number of unique active users over a 30-day rolling window.

Operation `kpi.mau.data-series.get`.

| Option | Required | |
|---|---|---|
| `--length <value>` | no | how many units before ending_at the series covers — days, or hours where the endpoint takes --unit. The cap differs by endpoint: 100 for most, 14 for Canvas (e.g. 7) |
| `--ending-at <value>` | no | date the series ends on, ISO-8601. Defaults to the time of the request (e.g. 2018-06-28T23:59:59-05:00) |
| `--app-id <value>` | no | app API identifier, to limit the analytics to one app |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> kpi uninstalls data-series`

Use this endpoint to retrieve a daily series of the total number of uninstalls on each date.

Operation `kpi.uninstalls.data-series.get`.

| Option | Required | |
|---|---|---|
| `--length <value>` | no | how many units before ending_at the series covers — days, or hours where the endpoint takes --unit. The cap differs by endpoint: 100 for most, 14 for Canvas (e.g. 14) |
| `--ending-at <value>` | no | date the series ends on, ISO-8601. Defaults to the time of the request (e.g. 2018-06-28T23:59:59-5:00) |
| `--app-id <value>` | no | app API identifier, to limit the analytics to one app |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> feed data-series`

Use this endpoint to retrieve a daily series of engagement stats for a card over time.

Operation `feed.data-series.get`.

| Option | Required | |
|---|---|---|
| `--card-id <value>` | no | news feed card API identifier |
| `--length <value>` | no | how many units before ending_at the series covers — days, or hours where the endpoint takes --unit. The cap differs by endpoint: 100 for most, 14 for Canvas (e.g. 14) |
| `--unit <value>` | no | time between data points: day or hour. Defaults to day (e.g. day) |
| `--ending-at <value>` | no | date the series ends on, ISO-8601. Defaults to the time of the request (e.g. 2018-06-28T23:59:59-5:00) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> feed details`

Use this endpoint to retrieve relevant information on a card, which can be identified by the card_id .

Operation `feed.details.get`.

| Option | Required | |
|---|---|---|
| `--card-id <value>` | no | news feed card API identifier |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> feed list`

Use this endpoint to export a list of News Feed cards, each of which will include its name and card API identifier.

Operation `feed.list.get`.

| Option | Required | |
|---|---|---|
| `--page <value>` | no | which page to return, counting from 0. A page holds up to 100 (e.g. 1) |
| `--include-archived <value>` | no | include archived items. Defaults to false (e.g. true) |
| `--sort-direction <value>` | no | desc for newest first, asc for oldest first. Defaults to oldest first (e.g. desc) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> purchases product-list`

Use this endpoint to return a paginated lists of product IDs.

Operation `purchases.product-list.get`.

| Option | Required | |
|---|---|---|
| `--page <value>` | no | which page to return, counting from 0. A page holds up to 100 (e.g. 1) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> purchases quantity-series`

Use this endpoint to return the total number of purchases in your app over a time range.

Operation `purchases.quantity-series.get`.

| Option | Required | |
|---|---|---|
| `--ending-at <value>` | no | date the series ends on, ISO-8601. Defaults to the time of the request (e.g. 2018-06-28T23:59:59-5:00) |
| `--length <value>` | no | how many units before ending_at the series covers — days, or hours where the endpoint takes --unit. The cap differs by endpoint: 100 for most, 14 for Canvas (e.g. 100) |
| `--unit <value>` | no | time between data points: day or hour. Defaults to day (e.g. 14) |
| `--app-id <value>` | no | app API identifier, to limit the analytics to one app |
| `--product <value>` | no | product name to report on (e.g. name) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> purchases revenue-series`

Use this endpoint to return the total money spent in your app over a time range.

Operation `purchases.revenue-series.get`.

| Option | Required | |
|---|---|---|
| `--ending-at <value>` | no | date the series ends on, ISO-8601. Defaults to the time of the request (e.g. 2018-06-28T23:59:59-5:00) |
| `--length <value>` | no | how many units before ending_at the series covers — days, or hours where the endpoint takes --unit. The cap differs by endpoint: 100 for most, 14 for Canvas (e.g. 100) |
| `--unit <value>` | no | time between data points: day or hour. Defaults to day (e.g. 14) |
| `--app-id <value>` | no | app API identifier, to limit the analytics to one app |
| `--product <value>` | no | product name to report on (e.g. name) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> segments list`

Use this endpoint to export a list of segments, each of which will include its name, Segment API identifier, and whether it has analytics tracking enabled.

Operation `segments.list.get`.

| Option | Required | |
|---|---|---|
| `--page <value>` | no | which page to return, counting from 0. A page holds up to 100 (e.g. 1) |
| `--sort-direction <value>` | no | desc for newest first, asc for oldest first. Defaults to oldest first (e.g. desc) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> segments data-series`

Use this endpoint to retrieve a daily series of the estimated size of a segment over time.

Operation `segments.data-series.get`.

| Option | Required | |
|---|---|---|
| `--segment-id <value>` | no | segment API identifier, to limit the analytics to one analytics-enabled segment |
| `--length <value>` | no | how many units before ending_at the series covers — days, or hours where the endpoint takes --unit. The cap differs by endpoint: 100 for most, 14 for Canvas (e.g. 14) |
| `--ending-at <value>` | no | date the series ends on, ISO-8601. Defaults to the time of the request (e.g. 2018-06-27T23:59:59-5:00) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> segments details`

Use this endpoint to retrieve relevant information on a segment, which can be identified by the segment_id .

Operation `segments.details.get`.

| Option | Required | |
|---|---|---|
| `--segment-id <value>` | no | segment API identifier, to limit the analytics to one analytics-enabled segment |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> sessions data-series`

Use this endpoint to retrieve a series of the number of sessions for your app over a designated time period.

Operation `sessions.data-series.get`.

| Option | Required | |
|---|---|---|
| `--length <value>` | no | how many units before ending_at the series covers — days, or hours where the endpoint takes --unit. The cap differs by endpoint: 100 for most, 14 for Canvas (e.g. 14) |
| `--unit <value>` | no | time between data points: day or hour. Defaults to day (e.g. day) |
| `--ending-at <value>` | no | date the series ends on, ISO-8601. Defaults to the time of the request (e.g. 2018-06-28T23:59:59-5:00) |
| `--app-id <value>` | no | app API identifier, to limit the analytics to one app |
| `--segment-id <value>` | no | segment API identifier, to limit the analytics to one analytics-enabled segment |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> users export ids`

Use this endpoint to export data from any user profile by specifying a user identifier.

Operation `users.export.ids.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> users export segment`

Use this endpoint to export all the users within a segment.

Operation `users.export.segment.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> users export global-control-group`

Use this endpoint to export all users within a Global Control Group.

Operation `users.export.global-control-group.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> users external-ids rename`

Use this endpoint to rename your users’ external IDs.

Operation `users.external-ids.rename.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> users external-ids remove`

Use this endpoint to remove your users' old deprecated external IDs.

Operation `users.external-ids.remove.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> users alias new`

Use this endpoint to add new user aliases for existing identified users, or to create new unidentified users.

Operation `users.alias.new.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> users alias update`

Use this endpoint to update existing user aliases.

Operation `users.alias.update.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> users identify`

Use this endpoint to identify an unidentified (alias-only or email-only) user using the provided external ID.

Operation `users.identify.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> users track`

Use this endpoint to record custom events, purchases, and update user profile attributes.

Operation `users.track.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> users delete`

Use this endpoint to delete any user profile by specifying a known user identifier.

Operation `users.delete.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> users merge`

Use this endpoint to merge one user into another user.

Operation `users.merge.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> messages live-activity update`

Use this endpoint to update and end Live Activities displayed by your iOS app.

Operation `messages.live-activity.update.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> messages scheduled-broadcasts`

Use this endpoint to return a JSON list of information about scheduled campaigns and entry Canvases between now and a designated end_time specified in the request.

Operation `messages.scheduled-broadcasts.get`.

| Option | Required | |
|---|---|---|
| `--end-time <value>` | no | end of the time range, ISO-8601 (e.g. 2018-09-01T00:00:00-04:00) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> messages schedule delete`

Use this endpoint to cancel a message that you previously scheduled before it has been sent.

Operation `messages.schedule.delete.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> messages schedule create`

Use this endpoint to schedule a campaign, Canvas, or other message to be sent at a designated time and provides you with an identifier to reference that message for updates.

Operation `messages.schedule.create.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> messages schedule update`

Use this endpoint to update scheduled messages.

Operation `messages.schedule.update.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> messages send`

Use this endpoint to send immediate messages to designated users via the Braze API.

Operation `messages.send.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> transactional v1 campaigns send`

Use this endpoint to send immediate, one-off transactional messages to a designated user.

Operation `transactional.v1.campaigns.by-id.send.create`.

| Option | Required | |
|---|---|---|
| `--campaign-id <value>` | yes | path parameter {campaign_id} |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> preference-center v1 url`

Use this endpoint to generate a URL for a preference center.

Operation `preference-center.v1.by-id.url.by-id.get`.

| Option | Required | |
|---|---|---|
| `--preferencecenterexternalid <value>` | yes | path parameter {PreferenceCenterExternalID} |
| `--userid <value>` | yes | path parameter {UserID} |
| `--preference-center-api-id <value>` | no | preference centre API identifier |
| `--external-id <value>` | no | your own identifier for the user, the one you send in users track |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> preference-center v1 list`

Use this endpoint to list your available preference centers.

Operation `preference-center.v1.list.get`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> preference-center v1 get`

Use this endpoint to view the details for your preference centers, including when it was created and updated.

Operation `preference-center.v1.by-id.get`.

| Option | Required | |
|---|---|---|
| `--preferencecenterexternalid <value>` | yes | path parameter {PreferenceCenterExternalID} |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> preference-center v1 create`

Use this endpoint to create a preference center to allow users to manage their notification preferences for your email campaigns.

Operation `preference-center.v1.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> preference-center v1 replace`

Use this endpoint to update a preference center.

Operation `preference-center.v1.by-id.replace`.

| Option | Required | |
|---|---|---|
| `--preferencecenterexternalid <value>` | yes | path parameter {PreferenceCenterExternalID} |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> scim v2 users delete`

Use this endpoint to permanently delete an existing dashboard user by specifying the resource id returned by the SCIM code POST /code method.

Operation `scim.v2.users.by-id.delete`.

| Option | Required | |
|---|---|---|
| `--id <value>` | yes | path parameter {id} |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> scim v2 users list`

Use this endpoint to look up an existing dashboard user account by specifying their email in the filter query parameter.

Operation `scim.v2.users.get`.

| Option | Required | |
|---|---|---|
| `--filter <value>` | no | a SCIM filter expression, such as userName eq "someone@example.com" (e.g. {userName@example.com}) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> scim v2 users get`

Use this endpoint to look up an existing dashboard user account by specifying the resource id returned by the SCIM code POST /code method.

Operation `scim.v2.users.by-id.get`.

| Option | Required | |
|---|---|---|
| `--id <value>` | yes | path parameter {id} |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> scim v2 users create`

Use this endpoint to create a new dashboard user account by specifying email, given and family names, permissions (for setting permissions at the company, app group, and team level).

Operation `scim.v2.users.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> scim v2 users replace`

Use this endpoint to update an existing dashboard user account by specifying the resource id returned by the SCIM code POST /code method.

Operation `scim.v2.users.by-id.replace`.

| Option | Required | |
|---|---|---|
| `--id <value>` | yes | path parameter {id} |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> sms invalid-phone-numbers get`

Use this endpoint to pull a list of phone numbers that have been deemed “invalid” within a certain time frame.

Operation `sms.invalid-phone-numbers.get`.

| Option | Required | |
|---|---|---|
| `--start-date <value>` | no | first day of the range, as YYYY-MM-DD. Must be earlier than --end-date (e.g. 2018-09-01) |
| `--end-date <value>` | no | last day of the range, as YYYY-MM-DD (e.g. 2018-09-01) |
| `--limit <value>` | no | how many results to return. Defaults to 100; the cap differs by endpoint — 500 for email lists, 1000 for Content Blocks (e.g. 100) |
| `--offset <value>` | no | how many results to skip before returning the rest (e.g. 1) |
| `--phone-numbers <value>` | no | a phone number in E.164 form, such as 12345678901 (e.g. 12345678901) |
| `--reason <value>` | no | why the number was marked invalid, such as provider_error (e.g. provider_error) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> sms invalid-phone-numbers remove`

Use this endpoint to remove “invalid” phone numbers from Braze’s invalid list.

Operation `sms.invalid-phone-numbers.remove.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> subscription status get`

Use this endpoint to get the subscription state of a user in a subscription group.

Operation `subscription.status.get.get`.

| Option | Required | |
|---|---|---|
| `--subscription-group-id <value>` | no | subscription group API identifier |
| `--external-id <value>` | no | your own identifier for the user, the one you send in users track |
| `--email <value>` | no | an email address. Where it is a filter, Braze answers for that address and ignores the date range (e.g. example@braze.com) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> subscription status set`

Use this endpoint to batch update the subscription state of up to 50 users on the Braze dashboard.

Operation `subscription.status.set.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> subscription user status`

Use this endpoint to list and get the subscription groups of a certain user.

Operation `subscription.user.status.get`.

| Option | Required | |
|---|---|---|
| `--external-id <value>` | no | your own identifier for the user, the one you send in users track |
| `--email <value>` | no | an email address. Where it is a filter, Braze answers for that address and ignores the date range (e.g. example@braze.com) |
| `--limit <value>` | no | how many results to return. Defaults to 100; the cap differs by endpoint — 500 for email lists, 1000 for Content Blocks (e.g. 100) |
| `--offset <value>` | no | how many results to skip before returning the rest (e.g. 0) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> v2 subscription status set`

Use this endpoint to batch update the subscription state of up to 50 users on the Braze dashboard.

Operation `v2.subscription.status.set.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> content-blocks list`

Use this endpoint to list your existing Content Blocks information.

Operation `content-blocks.list.get`.

| Option | Required | |
|---|---|---|
| `--modified-after <value>` | no | only items updated at or after this time, ISO-8601 (e.g. 2020-01-01T01:01:01.000000) |
| `--modified-before <value>` | no | only items updated at or before this time, ISO-8601 (e.g. 2020-02-01T01:01:01.000000) |
| `--limit <value>` | no | how many results to return. Defaults to 100; the cap differs by endpoint — 500 for email lists, 1000 for Content Blocks (e.g. 100) |
| `--offset <value>` | no | how many results to skip before returning the rest (e.g. 1) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> content-blocks info`

Use this endpoint to call information for your existing Content Blocks .

Operation `content-blocks.info.get`.

| Option | Required | |
|---|---|---|
| `--content-block-id <value>` | no | Content Block API identifier |
| `--include-inclusion-data <value>` | no | also return which campaigns and Canvases use this Content Block (e.g. false) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> content-blocks create`

Use this endpoint to create a Content Block .

Operation `content-blocks.create.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> content-blocks update`

Use this endpoint to update a Content Block .

Operation `content-blocks.update.create`.

| Option | Required | |
|---|---|---|
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> templates email list`

Use this endpoint to get a list of available templates in your Braze account.

Operation `templates.email.list.get`.

| Option | Required | |
|---|---|---|
| `--modified-after <value>` | no | only items updated at or after this time, ISO-8601 (e.g. 2020-01-01T01:01:01.000000) |
| `--modified-before <value>` | no | only items updated at or before this time, ISO-8601 (e.g. 2020-02-01T01:01:01.000000) |
| `--limit <value>` | no | how many results to return. Defaults to 100; the cap differs by endpoint — 500 for email lists, 1000 for Content Blocks (e.g. 1) |
| `--offset <value>` | no | how many results to skip before returning the rest (e.g. 0) |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> templates email info`

Use this endpoint to get information on your email templates.

Operation `templates.email.info.get`.

| Option | Required | |
|---|---|---|
| `--email-template-id <value>` | no | email template API identifier |
| `--query <key=value>` | no | any other query parameter, repeatable |

### `braze <profile> templates email create`

Use this endpoint to create email templates on the Braze dashboard.

Operation `templates.email.create.create`.

| Option | Required | |
|---|---|---|
| `--template-name <value>` | no | name of the email template |
| `--subject <value>` | no | subject line of the email |
| `--body <value>` | no | HTML body of the email |
| `--plaintext-body <value>` | no | plain-text version of the body, for clients that will not render HTML |
| `--preheader <value>` | no | preview text an inbox shows after the subject line |
| `--tags <value>` | no | tags to attach. They must already exist in Braze |
| `--should-inline-css <value>` | no | write the CSS into the markup itself rather than leaving it in a stylesheet |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |

### `braze <profile> templates email update`

Use this endpoint to update email templates on the Braze dashboard.

Operation `templates.email.update.create`.

| Option | Required | |
|---|---|---|
| `--email-template-id <value>` | no | email template API identifier |
| `--template-name <value>` | no | name of the email template |
| `--subject <value>` | no | subject line of the email |
| `--body <value>` | no | HTML body of the email |
| `--plaintext-body <value>` | no | plain-text version of the body, for clients that will not render HTML |
| `--preheader <value>` | no | preview text an inbox shows after the subject line |
| `--tags <value>` | no | tags to attach. They must already exist in Braze |
| `--should-inline-css <value>` | no | write the CSS into the markup itself rather than leaving it in a stylesheet |
| `--query <key=value>` | no | any other query parameter, repeatable |
| `--input <source>` | no | request body: @file, - for stdin, or inline JSON |
