# Closed backlog items

Where a number went when it left [`BACKLOG.md`](BACKLOG.md). One line each, newest first. Nothing
is ever removed from this file.

| Number | Task | Closed | Commit |
|---|---|---|---|
| `CORE-16` | `packages/cli` takes streams, rendering, exit codes, keyring, config loading, paths, credentials and the run log from [`@leemour/cli-core`](https://www.npmjs.com/package/@leemour/cli-core) instead of its own copies, and gains `braze complete` (`OPS-3`) and `braze update`. Same exit codes, JSON fields, keyring addresses and directories as before. A `credentials.json` written as `apiKey` gains `secret` beside it once, `0600`, so an older braze still reads it. The run log now survives its directory vanishing mid-run (an unhandled `ENOENT` before — reproduced) and redacts `BRAZE_API_KEY`. Four pull requests, #38–#41 | 2026-09-24 | see `git log --grep CORE-16` |
| `OPS-3` | `braze complete zsh\|bash\|fish\|powershell` prints the script to source; the script runs the hidden `braze complete -- <words>` on every Tab, which answers from cli-core's command registry — commands, options and profile names from the local config, never anything from Braze. Part of `CORE-16` | 2026-09-24 | see `git log -- packages/cli/src/commands/complete.ts` |
| `OPS-6` | Publish from CI. `.github/workflows/release.yml` publishes through npm's trusted publishing — no npm token stored anywhere, provenance attached — and tags and writes the GitHub release only once npm shows the version. `pnpm pack` then `npm publish <tarball>`: pnpm rewrites `workspace:*`, npm is the one that can use trusted publishing. `scripts/release.sh` starts the workflow and proves the result from the registry; `--local` keeps the keyring path. Overturns `NEED-49` | 2026-09-24 | see `git log -- .github/workflows/release.yml` |
| `CORE-11` | One `userAgent()` in `packages/cli/src/user-agent.ts` instead of the same string at two call sites, and the runtime read rather than assumed. `node` is the **fallback**, not the first check: bun reports a `process.versions.node` of its own — measured at 24.3.0 under bun 1.3.14 — so a node-first test would label every bun run as node | 2026-09-23 | see `git log -- packages/cli/src/user-agent.ts` |
| `BULK-10` | `braze runs cleanup --older-than <days>`, with `--dry-run` and `--confirm`. Opt-in every time and no retention setting in the config file, deliberately (`NEED-3`): a value set once and forgotten is how a timer gets reinvented. A directory whose `run.json` cannot be read is never removed — that file is written before the first request, so an unreadable one is the run somebody wants to look at | 2026-09-23 | see `git log -- packages/cli/src/commands/runs.ts` |
| `DOC-3` | [`docs/development.md`](development.md). Written earlier than the "second contributor" trigger this item carried, because the gap was different from the one assumed: what a contributor needs — the gates and what each catches, the generated files, the plan-first and backlog-number rules — lived only in `docs_ai/HANDOFF.md`, which is not in git. A fresh clone had the README's five commands and nothing else | 2026-09-23 | see `git log -- docs/development.md` |
| `SEC-2` | Text from Braze and from a customer's input file is neutralised where a person reads it: `packages/cli/src/output/sanitize.ts`, applied in the pretty renderer, in every diagnostic on stderr and in every column of `records.csv`. Control characters are made **visible** (`\x1b`) rather than dropped — a reader has to know the value carried something strange. Free text in the audit gets the spreadsheet formula guard; identifier columns deliberately do not, because a prefixed id no longer joins back to the input, and that residual risk is named in [`security.md`](security.md). Machine output is byte-identical, asserted on the built binary: `JSON.stringify` already escapes these and those bytes are a contract | 2026-09-23 | see `git log -- packages/cli/src/output/sanitize.ts` |
| `CLI-12` | Every failure a script can meet carries one of the fourteen codes in `packages/cli/src/exit-codes.ts`. The remainder closed here: an unparseable or schema-invalid `config.json`, and `credentialStorage: "keyring"` on a machine whose keyring does not work, both exited 1 as `generic_failure` while `docs/agents.md` published `3 → configuration_error` as a contract | 2026-09-23 | `3f6924e` |
| `CAT-1` `CAT-2` `CAT-3` `CAT-4` `CAT-5` `CAT-6` | The generated catalog. `pnpm spec:sync` downloads Braze's own Postman collection (99 requests) into `spec/`; `pnpm catalog:generate` turns it into 95 operations in `packages/core/src/operations/generated.ts` with deterministic ids; `operations/overrides.ts` corrects it by operation id; `docs/catalog-coverage.md` is generated alongside and gated by `pnpm catalog:check`; all 95 are registered as typed commands in one loop. The source is settled in `NEED-13`; `FIND-13` (`/users/export/ids` is a read Braze implements as a POST) was the first override | 2026-09-14 | see `git log -- packages/core/src/operations` |
| `OPS-2` | Published **`@leemour/brazecli@0.1.0`** — scoped, because npm refused the unscoped name as too similar to an unrelated `braze-cli` (`NEED-48`) — one package, not two, and not v1. The owner overrode `NEED-2` on the version and `REQUIREMENTS.md` §2 on the split (`NEED-47`): `packages/core` stays private and is bundled into the command at build time. Version discipline is now a gate — `pnpm version:check` fails when the manifest, `version.ts` and the skill's frontmatter disagree — and `prepack` rebuilds, after a tarball shipped the compiler's entry point instead of the bundle (`BUG-16`) | 2026-09-17 | see `git log -- packages/cli/package.json` |
| `DOC-1` `DOC-2` | The front door and eight pages behind it: `README.md` rewritten for someone who has never seen this, plus `installation`, `authentication`, `configuration`, `usage`, `bulk`, `agents`, `security` and `troubleshooting` under `docs/`. `bulk-runs.md` landed as `bulk.md`, and `development.md` stayed in the README as `DOC-3` until there is a second contributor | 2026-09-17 | see `git log -- docs/` |
| `CLI-15` | `braze profile add --key-stdin` reads the API key from standard input, so a CI with neither a TTY nor `BRAZE_API_KEY` can configure a profile — this tool is built for automation first and could not be automated at its first step. Explicit rather than "read stdin when it is not a terminal": the command reads input for nothing else, so a stray pipe would otherwise install a key nobody meant to give. Trimmed, because `echo "$KEY" |` adds a newline that fails auth in a way that reads as a wrong key | 2026-09-17 | see `git log -- packages/cli/src/commands/profile.ts` |
| `BULK-9` | The million-record proof: peak resident records measured at **599** against the predicted ceiling of `concurrency * 2 * batchSize` = 600, and a bounded heap through a million records end to end, parse to CSV. The heap half lives in the CLI package because core may not touch `process`; the fixture is a bare fetch stub, since `mockBraze` would hold a million user objects alive itself | 2026-09-15 | see `git log -- packages/core/src/bulk/memory.test.ts` |
| `BULK-5` `BULK-7` `BULK-8` `BULK-12` | The bulk interface and its artifacts: `--records` on any batching command, `records.csv` written per record as outcomes arrive, progress on stderr in pretty mode only, and the end-of-run summary as the **result** on stdout — one count per §34 status, `invalid` kept apart from `failed`, generated from the same tally as `run.json` so the two cannot disagree. Ctrl+C now waits for the run to wind down (`Run.onDrain`) instead of truncating the audit and printing nothing. Found `BUG-10`: a batch cancelled before it was sent was recorded as `failed`, claiming a refusal Braze never made. Verified end to end against the staging workspace | 2026-09-15 | see `git log -- packages/cli/src/bulk.ts` |
| `BULK-2` | Streaming parsers for JSONL and CSV, neither of which materializes the input: what gets read is the same for a 400 000-row file as for a 50 000-row one, and there is a test asserting exactly that. The field a file's records belong in is named by `--records-field` and **refused rather than guessed** when the operation batches several — an events file defaulted into `attributes` passes every check and lands as custom attributes called `name` and `time`. CSV values stay strings; JSON-array input deferred as `BULK-13` | 2026-09-15 | see `git log -- packages/cli/src/input/records.ts` |
| `BULK-6` | Truthful per-record status. A 2xx is not one verdict for 75 records: Braze names the objects it refused by `index` inside `input_array` — measured against staging on 2026-09-15, since its page documents neither field — so those records are `failed` with Braze's own words and the rest stay `submitted`. Every read of that shape is defensive: an entry we cannot place makes `unknown` the records it might be, never `failed`. The `*_processed` cross-check notes a discrepancy instead of condemning the batch, because it counts what Braze queued and one measurement is not an arithmetic guarantee. Closed `RISK-3` | 2026-09-15 | see `git log -- packages/core/src/bulk/verdict.ts` |
| `BULK-1` `BULK-3` `BULK-4` | The bulk executor: `AsyncIterable<BulkRecord>` in, `AsyncIterable<BulkOutcome>` out, batching to `batchTotal`, a `p-queue` bounded at `concurrency * 2` batches, and real backpressure — the consumer's demand is what advances the source. Found `BUG-9`: bounding the queue alone let a fast endpoint pull the whole input into an unbounded buffer | 2026-09-15 | see `git log -- packages/core/src/bulk` |
| `CLI-13` | `SIGINT`/`SIGTERM` finish the run file instead of dying mid-write. A run owns an `AbortController`; the handler cancels, awaits the idempotent `run.finish`, exits 130, and a second signal exits at once. Cancellation is decided from the signal rather than the error code, so the handler and the catch cannot race over the status — and `outcome_unknown` survives it (rule 4) | 2026-09-15 | see `git log -- packages/cli/src/runs/signals.ts` |
| `CORE-10` | Valibot validation with the three levels. `validateRequest` runs in `BrazeClient.execute` before the attempt loop and in the CLI before the write guards, so a refusal costs zero requests and `--dry-run` answers the question it was asked. `generated` checks only the body's JSON kind — an example is not a schema; `strict` needs a handwritten schema or the build fails; `braze api` stays `passthrough` as the escape hatch. Found `BUG-8` | 2026-09-14 | see `git log -- packages/core/src/validate.ts` |
| `CAT-8` | `pnpm docs:generate` writes `docs/commands.md` from the live Commander tree — 105 commands, handwritten and generated alike — and `pnpm docs:check` gates it in CI, naming the first line that went stale. Renders the same `describeProgram` tree as `braze commands --json`, so the two cannot disagree | 2026-09-14 | see `git log -- scripts/generate-commands-doc.mjs` |
| `CAT-11` | Pagination: `--paginate`, `--max-pages`, `--max-items`, always bounded — a default ceiling of 10 pages when nothing else says otherwise. Closed `FIND-19`: three paged endpoints were unclassified, and Braze documents 250 per page for event names and no page size at all for two others | 2026-09-14 | see `git log -- packages/cli/src/execute.ts` |
| `CAT-7` | `braze schema <operation>` — the contract of one operation, addressable by command words or by id, needing no profile. The generator now extracts request bodies (32 real examples, 14 annotated prose, `FIND-17`), and `commands --json` carries `operationId` on every generated leaf so the id is reachable at all | 2026-09-14 | see `git log -- packages/cli/src/commands/schema.ts` |
| `CAT-13` | Help text that informs: `packages/core/src/operations/parameters.ts`, 43 descriptions keyed by parameter name, covering all 134 slots in the catalog so no flag reads `query parameter` (`UX-5`). A gate fails the build on an undescribed parameter. The `BUG-6` half landed with `CAT-9` | 2026-09-14 | see `git log -- packages/core/src/operations/parameters.ts` |
| `CAT-9` | Contract tests over every generated operation — 95 parameterised cases plus six shared properties, 329 tests total. Found `UX-6` (five SCIM commands needed a capital letter, and the wrong spelling printed help instead of erroring) and `BUG-6` (Braze describes the catalog PATCH with the DELETE's sentence) | 2026-09-14 | see `git log -- packages/core/src/operations/catalog.test.ts` |
| `CLI-14` | `braze runs list/show/path`, needing no profile and no key — the point is to read what happened when the configuration is what went wrong | 2026-09-13 | see `git log -- packages/cli/src/commands/runs.ts` |
| `CLI-10` | `--dry-run`: resolve, validate, construct, count, send nothing. Allowed even on a read-only profile | 2026-09-13 | see `git log -- packages/cli/src/commands/api.ts` |
| `CLI-9` | `braze api <METHOD> <path>` — relative Braze paths only, writes gated, repeated `--query` refused with a message naming `CAT-3` | 2026-09-13 | see `git log -- packages/cli/src/commands/api.ts` |
| `CLI-8` | Run directories: `run.json` written at the start and finalized on every path, atomically, `0600` | 2026-09-13 | see `git log -- packages/cli/src/runs` |
| `CLI-7` | Pino into `events.jsonl` through a plain synchronous append stream — an async destination drops the tail on exit, which is the case the log exists for | 2026-09-13 | see `git log -- packages/cli/src/logging` |
| `CLI-6` | Pretty renderer: table, labelled fields, JSON fallback. Three shapes, not a renderer per endpoint | 2026-09-13 | see `git log -- packages/cli/src/output/pretty.ts` |
| `CLI-5` | Output modes, with every diagnostic on stderr in **every** mode, and the invariant tested on the built binary | 2026-09-13 | see `git log -- tests/machine-output.test.ts` |
| `CLI-11` | Input from `@file`, `-` for stdin, or inline JSON — parsed and refused before a command can half-send it | 2026-09-13 | see `git log -- packages/cli/src/input` |
| `CLI-4` | Credentials: OS keyring first with a single warned fallback to a `0600` file in a `0700` directory, written atomically. The keyring is reached through one injected seam, so no test can touch a real keychain | 2026-09-13 | see `git log -- packages/cli/src/auth` |
| `CLI-3` | `braze profile add/list/remove`. The key is never a command line argument and never printed, masked or otherwise | 2026-09-13 | see `git log -- packages/cli/src/commands/profile.ts` |
| `CLI-2` | Configuration hierarchy in one function — CLI option > environment > profile > global > default — with `env-paths` for locations and a valibot schema that names the offending field | 2026-09-13 | see `git log -- packages/cli/src/settings.ts` |
| `CLI-1` | Commander bootstrap, the global flags, and failures turned into one stable exit code per error code | 2026-09-13 | see `git log -- packages/cli/src/program.ts` |
| `CORE-9` | Operation metadata — access, permission, `retryPolicy`, batch limits, pagination style; `rawOperation` for `braze api`. Retry branches on the policy alone, so there is one source of truth | 2026-09-13 | see `git log -- packages/core/src/operation.ts` |
| `CORE-7` | Ambiguous writes: a write that produced no response becomes `outcome_unknown` with `retryable: false` stated explicitly, never `failed` | 2026-09-13 | see `git log -- packages/core/src/client.ts` |
| `CORE-6` | Rate limits: `Retry-After` in both its forms and `X-RateLimit-Reset` beat our backoff; beyond 30 s a structured `rate_limited` comes back instead of a blocked caller | 2026-09-13 | see `git log -- packages/core/src/retry.ts` |
| `CORE-5` | Retry engine: one retry by default, reads only, full jitter over `[0, min(cap, base·2^n))`, deterministic under an injected `random` | 2026-09-13 | see `git log -- packages/core/src/retry.ts` |
| `CORE-4` | Error normalization: HTTP status and Braze's error message into the closed code list | 2026-09-13 | see `git log -- packages/core/src/retry.ts` |
| `CORE-8` | Request identity and timing: a UUID per attempt, and `attempts`, `totalDurationMs`, `retryWaitMs` on every result, all from the monotonic clock | 2026-09-13 | see `git log -- packages/core/src/client.ts` |
| `CORE-3` | Per-attempt timeout: the client composes its own `AbortController` and fires it through the injected `sleep`, so a timeout test waits for nothing. The timer is cancelled when the response arrives | 2026-09-13 | see `git log -- packages/core/src/client.ts` |
| `CORE-2` | Request construction: path parameters, query serialization, JSON body, and the refusals — an absolute URL where a path belongs, a non-https endpoint, an unsupplied placeholder | 2026-09-13 | see `git log -- packages/core/src/request.ts` |
| `CORE-1` | `BrazeClient.send` — one attempt with injected `fetch`, `sleep`, `clock`, `now`, `random` and `logger`; bearer auth; any HTTP status returned rather than classified | 2026-09-13 | see `git log -- packages/core/src/client.ts` |
| `CORE-12` | Mock Braze test kit — scripted `fetch` covering every status the brief names, invalid JSON, network failure, a connection dropped after send, and a request that hangs until aborted. Published as `brazecli-core/testing` | 2026-09-13 | see `git log -- packages/core/src/testing` |
| `OPS-1` | Repository scaffold: pnpm workspace, two packages, Biome, TypeScript strict, vitest, lefthook, CI, the three-layer core portability gate, documentation structure | 2026-09-13 | see `git log -- package.json` |
